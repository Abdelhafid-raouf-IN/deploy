base de donnes : 
INSERT INTO swagger_urls (url) VALUES ('http://localhost:9090/v3/api-docs/unibank-service-pilot');

creationbasedonnees :
CREATE TABLE swagger_urls (
    id SERIAL PRIMARY KEY,
    url VARCHAR(255) NOT NULL
);
